# 古代战争 3.0 v1.2.5

本版本修复 RTS 军队在大步长调度下的行军、交战交接、战后巡逻与禁卫军任务连续性问题。

## RTS 大步长行军

- 修复 RTS 完整共享路线写入原版 `local_path` 后失去优先移动脉冲的问题。此前大步长跳过批次只处理路径桥接所有权，导致 RTS 士兵的路径长期停在首步，反复补命令仍不前进。
- 跳过批次现在仅额外执行已由 RTS 状态验证的共享原生路线，以及原有桥接路径；普通军队不会被提升为每帧路径更新。
- 共享路线资格在主线程预先快照，后台批次不读取 RTS 可变状态，避免引入控制器字典并发访问。

## 进攻与和平接管

- 目标城市领土/边界进入战斗时，RTS 会先把攻击目标和攻击 zone 写入进攻方城市，再释放给原版城市战斗 AI。
- RTS 目标完成或城市控制完成会立即失效控制器并刷新全部军队成员的和平岗位，边境巡逻 job 可重新接管，不再被 RTS 任务持续覆盖而落入 `social`。
- 增加目标城市交战交接诊断日志，便于后续从 `Player.log` 跟踪军队、来源城市、目标城市和攻击 zone。

## 禁卫军

- 禁卫军从所有 RTS 所有权入口排除，包括禁卫军队长、国王和领主边界情况。
- 通用决策写入前强制恢复禁卫军保护/跟随国王任务，防止原版决策将其改写为社交等无关行为。

## 验证

- 主项目构建通过，0 警告、0 错误。
- RTS 战争生命周期规则、任务连续性、禁卫军任务展示和共享路线源码守卫通过。
- 包含本地部署 SHA-256 逐文件校验。战场运行日志验收仍建议在大步长战争与战后和平场景中继续观察。

## 安装说明

- 下载 `AncientWarfare3-v1.2.5.zip`。
- 解压后应得到单一目录 `AncientWarfare3.0`，将该目录放入 NeoModLoader 的模组目录。
- 更新前请移除旧的 `AncientWarfare3.0` 模组目录，避免旧源码残留或出现相同 GUID 的重复模组目录。
- 本发布包为源码加载包，不包含预编译的 `AncientWarfare3.dll`。
- 包内保留 `Assemblies` 及其依赖 DLL，NeoModLoader 编译源码所需依赖不可删除。

# 古代战争 3.0 v1.2.2

本次版本集中修复继位稳定性、调度性能、RTS 战时动员，以及部分东方类种族的姓名规则。

## 继位与王室身份修复

- 修复已登记继承人在即位前后的身份校验与王国、城市、军队归属衔接。
- 修复国王死亡或继位完成后 RTS 军队可能失去调度、停留在等待军令状态的问题。
- 加强继位重试与幂等保护，避免同一继位流程重复清理、重复迁移或重复写入。
- 修复继承流程中候选人、王室关系和姓名身份可能不同步的问题。

## 性能与调度修复

- 将高开销的王国、继位与战时维护工作改为有预算的分批调度，避免单帧集中扫描。
- 修复大步长调度下逻辑回合重复推进或遗漏推进的问题，保证同一逻辑步骤只执行一次。
- 对战时动员、读档恢复和状态对账使用有界队列与缓存代际，减少全量 Actor、城市和王国扫描。
- 保留 Unity 对象写入在主线程提交阶段，避免后台任务与世界状态交叉写入造成卡顿或状态异常。

## RTS 军队与战时动员修复

- 修复开战首轮 RTS 军队未及时获得任务、双方军队长时间等待军令的问题。
- RTS 动员改为按城市征兵比例计算的数值账本，生成临时征召兵直接补入将领军队，不再在战备期扫描真实居民。
- 临时征召兵仅参与战斗和基本温饱，不参与生产、繁殖、继承或其他平民任务；战后按账本分批复员清除。
- 修复军队创建、读档、继位和大步长调度后的成员对账与任务恢复，避免军队人数、任务和实际成员脱节。
- 恢复原版征兵系统的正常所有权与调用边界，避免模组侧重复接管导致征兵异常。

## 东方类种族命名修复

- 狗、狐狸、柠檬人、兔子、乌龟现按与猴族一致的夏式姓名规则命名：姓前名后，并从现有中文词库取词。
- 补全上述种族的出生、存档读取、族谱与分支身份归档，避免旧存档角色缺失结构化姓名或族谱入口。
- 修复夏化与原生夏式命名在姓名格式、姓氏继承和显示上的不一致。

## 安装说明

- 下载 `AncientWarfare3-v1.2.2.zip`。
- 解压后应得到单一目录 `AncientWarfare3.0`，将该目录放入 NeoModLoader 的模组目录。
- 本发布包为源码加载包，不包含预编译的 `AncientWarfare3.dll`。
- 包内保留 `Assemblies` 及其依赖 DLL，源码编译加载所需依赖不可删除。

## 验证范围

- Release 主项目构建。
- 完整规则测试与 RTS 对抗模拟。
- 源码部署结构校验。
- ZIP 根目录、运行时白名单、`Assemblies` 依赖、版本号与解压结构校验。

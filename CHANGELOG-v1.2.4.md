# 古代战争 3.0 v1.2.4

本次版本重点修复大步长模式下军队卡住不动的问题：并行敌对关系查询撞上原版非线程安全缓存、开坛讲学职官离任写入的可变字段谓词，以及寻路轮询握手可能让人物长期等待。同时延续 RTS 军队连续性方向，补强部署控制、耗损军队恢复和外交开战排序。

## 大步长卡住修复（重点）

- 修复大步长模式下并行军队目标搜索并发访问原版敌对关系缓存（`Kingdom.isEnemy`、`WarManager.isInWarWith`、`KingdomAsset.isFoe`）导致抛出异常、模拟被暂停、军队与国王领主整体停止不动的问题；改由无锁并发影子缓存提供敌对判定，战争状态变化和王国清理时同步失效。
- 修复开坛讲学等场景下职官离任写入以可变字段 `INSTITUTION_AT_APPOINTMENT` 作为匹配条件，字段在延迟提交前发生变化时匹配不到记录、抛出异常并在大步长下暂停整个模拟的问题；离任更新改为仅按不可变主键与身份列定位记录。
- 回退上一版引入的后台寻路轮询握手（`WaitingInitialized`/`HasWorkerUpdate`），避免 worker 未及时发布更新时人物长期停留在等待状态；后台寻路线程优先级恢复常规。

## RTS 军队连续性增强

- 军队部署除设置岗位外，同时校验并重申任务（进攻/集结），仅在岗位或任务缺失时重新接管，减少部署后被其他行为顶掉的情况。
- 放宽部署接管的存活与身份判定拆分，统帅按统帅规则处理，普通成员按战士且非禁卫判定，避免边界情况下漏接管。
- 战争总管在军队兵力耗尽但仍需作战时请求恢复，零可用野战军队的王国也能进入恢复流程，不再被直接判定为不可分配。
- 外交开战候选改为稳定排序（可用者优先、保持原有相对顺序），减少候选抖动。
- 地图军队快照补充发布统帅坐标，改善军队信息展示。

## 命名与显示修复

- 显姓、显氏手动改名统一走人物显式改名服务，归档显示名按 `name_integrated` 与原氏名重建，避免改名后归档显示名与活体不一致。
- 修正禁卫军保护国王与跟随国王任务的本地化键，使其显示正确文本。

## 诊断

- 模拟工作池在无捕获异常却仍存在未执行项时，附带停止索引游标与停止标记，便于定位后台模拟在何处静默中止。

## 安装说明

- 下载 `AncientWarfare3-v1.2.4.zip`。
- 解压后应得到单一目录 `AncientWarfare3.0`，将该目录放入 NeoModLoader 的模组目录。
- 更新前请移除旧的 `AncientWarfare3.0` 模组目录，避免旧源码残留或出现相同 GUID 的重复模组目录。
- 本发布包为源码加载包，不包含预编译的 `AncientWarfare3.dll`。
- 包内保留 `Assemblies` 及其依赖 DLL，NeoModLoader 编译源码所需依赖不可删除。

## 验证范围

- Release 主项目构建（0 警告 0 错误）。
- 规则测试全量通过。
- 敌对关系线程安全 patch 与原版 `isEnemy`/`isInWarWith`/`isFoe` 语义逐条比对。
- 开坛讲学职官离任守卫（`HistoricalSchoolRuntimeErrorSourceGuard`）通过。
- ZIP 单一根目录、源码白名单、版本号、依赖程序集及逐文件 SHA-256 校验。

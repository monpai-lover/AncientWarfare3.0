# 古代战争 3.0 v1.2.7c

v1.2.7b 之后的维护版本。全部是修复，没有新增系统。主要是三件事：一个会
弹窗暂停游戏的并发缺陷、一批被证实白做的运行时开销、以及婴儿头像与夏人
建筑贴图。

## 崩溃

- **修复「Simulation worker did not execute all scheduled work」弹窗暂停。**
  这是玩家反馈里最刺眼的一条。根因有两个，都在 `AWSimulationWorkerPool`：

  1. **跨代记账串位。** 两个协助入口（`TryAssistActiveOperation` /
     `...Until`，由模拟协调线程调用）先读代次判断、再单独 CAS 参与者计数，
     **这两步不是原子的**。于是一次协助可以「按上一代做的判断」把 +1 记到
     下一代的账上，而带代次守卫的减法会跳过它 —— 那一代的参与者数从此
     永远凑不齐 0，等待方挂死。游标是同样的问题：认领是「先自增再判断」，
     跨代的自增会**吃掉下一个操作的一个索引**然后因为范围对不上直接退出，
     那一项就再没人做。玩家日志里 `nextIndex=45` 越过 `endIndex=36`
     正是这个形状。
     现在参与者计数和索引游标都改成「代次 + 值」打包进一个 64 位字做 CAS，
     跨代的加入/退出/认领会被直接否决，一个索引都不会被误消费。

  2. **发布顺序导致计数被整片抹掉（弹窗的直接来源）。** 操作启动时在锁内
     **先**发布代次、**后**才把已执行计数清零；而协助入口不进这把锁，只凭
     代次就能加入并开始干活。于是存在这样一段窗口：协助线程已经把活全干完
     并累加了计数，主线程才执行清零 —— 活干了，账没了，收尾时拿到 `0/N`
     判定「有未执行项」并抛出。压力台上可以直接看到「动作实际被调用 6 次 /
     应为 6 次」却仍然报 `0/6`。
     现在代次是锁内最后一行写出的**唯一发布点**，任何外部线程能碰到的状态
     都在它之前初始化完毕；收尾则按发布的逆序拆除。

  **也就是说，这个弹窗在绝大多数情况下是误报**：模拟并没有真的丢工作，
  是记账被并发抹掉了。但它确实暴露了上面两个真实的并发错误，所以守卫本身
  保留不动。

  并发压力台（主线程连续发起操作 + 独立线程持续协助，正是协调线程的真实
  用法）：修复前约每 100 万次操作失败 1 次；修复后连续 776 万次操作，
  0 次记账不平、0 次丢工作。

- 修复箭塔相关的空引用崩溃：原版 `Kingdom.isCiv()` 是裸 `return asset.civ`，
  存档里 `asset` 为空时直接炸。读档时补上修复，并加固敌情缓存里的四处裸解
  引用。

## 卡顿

- **开坛讲学时的明显掉帧。** 学派写缓冲的提交跑在主线程的权威帧上，而
  SQLite 的 WAL 自动检查点恰好在**提交那一刻**触发 —— 实测单次提交 266ms，
  同一局里其余提交只有 0.2~0.4ms 的双峰分布。检查点本身省不掉，但它是纯
  I/O，没有理由占用主线程。改为关闭自动检查点，另起后台连接跑
  `wal_checkpoint(PASSIVE)`；存档时仍在主线程做一次确定性检查点兜底。

- **修复寻路拓扑的白做功。** 拓扑重建每次都先构造一份 26~28MB 的执行数据，
  之后才去问调度器收不收 —— 收不下就整份丢掉。现在先问再构造。同时修掉
  一条让拓扑**永远不更新**的分支（调度未启用时直接返回且不落标记）。
  实测该阶段分配从 54MB 降到 428KB。

- 收紧协作调度的时间片：单次后处理区间按时间收口（只在批次边界让出），
  突发截止改为每步都检查。原来的突发比例常数使上限值永远取不到，等于没有
  上限。

- 删除两处死代码：状态模拟调度器（616 行，从未接入）和路径复用探针
  （严格命中率 0.4%，文件头自己写着该删）。

## 头像与贴图

- **婴儿不再套用自定义头像。** 自定义头像是按成年比例画的，套在婴儿身上
  会明显不协调。现在婴儿一律走原版头。
- **夏人国王的高分辨率身体只对成年生效。** 原来只判断「是夏人 + 是国王」，
  婴儿国王会被拉伸。
- 更新 43 张夏人建筑贴图（`Xia` 22 张、`Xia_MandateCapital` 21 张）：民居
  五级、宫殿、兵营。

## 补遗

- **学派宗师的历史父母**：审计全部 84 位，确认表的稀疏**不是遗漏** —— 
  表头写着「宁缺毋滥」，只录史料确有其人的父名。按已有先例（刘邦之母
  「刘媪」只有称谓、不著其名）补上孟子之母「孟母」一条。其余 73 位两汉
  典籍确实不载父名，维持空缺。

- **修复 Harmony 补丁静默不生效。** HarmonyX 的 `PatchClassProcessor` 在类
  上缺少类级 `[HarmonyPatch]` 时会直接返回、一个方法都不打**且不报错**。
  有四个补丁类因此从未真正装载过。现在补齐类级特性，并让加载器检查
  `Patch()` 的返回值 —— 打不上就打警告（以 `[HarmonyPrepare]` 主动关闭的
  单独识别，不误报）。其中两个从未实机跑过的补丁
  （`AW_DirtyMetaActorIndexPatch`、`AW_SpecialGovernmentCombatPatch`）
  先显式停用，待验证后再开。

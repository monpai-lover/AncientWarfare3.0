# 古代战争 3.0 v1.2.7e

v1.2.7d 之后的修复版本。本版重做城市与州的改名交互，让州名成为稳定的持久化值，
并修复历史州郡县名字始终不生效的问题。同时修复三处会导致崩溃或数据泄漏的出生补丁。

## 城市名与州名

- 城市名横幅点开后拆成两个输入框：左为城市本名，右为州名。交互沿用人物「姓/氏 + 名」
  双框的既有设计 —— 平时显示合并后的单个名字，点击展开，失焦自动提交并收起。
- 改名走权威命令 `RenameCityState`，不再直接写 `City.data.name`。多人游戏下由主机
  裁决，州名的持久化与历史记录统一由服务层负责。任一步失败会回滚城市名。
- 州名成为权威持久化值。读取路径只返回存储的 `RegionName`，不再从首府城市名推导，
  存档往返与容错修复都不会改写玩家设定的州名。
- 只有「玩家发起的跟踪重命名 + 该城是州治 + 州治已锁定」三条同时满足时，改城市名
  才会带动州名变化。自动生成的改名不再影响州名。
- 城市不属于任何州时隐藏州名字段。
- 新增 `docs/api/city-state-rename.md`，说明命令用法与外部模组的兼容规则。

## 历史州郡县

- 修复开启「历史州郡县」后名字始终不生效的问题。此前创建州时写死的是「城市名 + 州」
  的拼接（「晋江」→「晋江州」），历史目录里真正的州名只在州名为空时才会填入，而创建
  时已经写了拼接名，那条路径永远轮不到。
- 修复历史州名与城市名互相等待的死锁。城市名多为随机生成，几乎撞不上历史县名，
  导致州的历史 ID 一直为空；而给城市取历史名又要靠这个 ID，两边都起不来。现在匹配
  不上时按稳定种子分配一个尚未被占用的历史州，打通取名链路。
- 修复行政级别后缀重复。历史库中州名自带「州」字、郡名自带「郡」字，存入前统一剥离
  ——此前会出现「并州州」这样的州名，以及地图上一座叫「五原郡」的城市。
- 历史地名去重范围从「同一州内」扩大到全图。历史地名在整张地图上唯一，此前不同州会
  各自挑走同一个「雒阳」。
- 历史名用尽后退回随机生成名，不循环复用。

## 出生与继承

- 修复合成兵繁殖拦截导致的空引用崩溃。神迹、孢子等单亲生育路径的调用方会直接在返回值
  上链式调用，拦截时返回 null 会立即崩溃。现在只拦截双亲都在场的有性繁殖。
- 修复平民即时生育绕过合成兵拦截的问题。该路径直接调用生育并接管原版怀孕状态，
  原有的拦截补丁在这条路上从不执行。
- 修复合成兵后代被写入继承索引且不清理的问题。
- 贵族生男偏好扩大到更广的范围。此前只有已激活求子请求的夫妻才生效，现在国王、
  法定继承人、封地王子及男性可继承爵位持有者在没有在世儿子时同样适用。
- 生下女儿后立即为父方请求再孕，不再等待下一个年度周期。

## 其他

- 国王手持物锚点上移两像素，地图与检视面板同步。
- 补齐城市/州改名相关的全部本地化条目（简体中文、英文、繁体中文）。

## 已知限制

- 上述历史名与后缀修复只对新建的州和新城市生效。已有存档中已经叫「晋江州」「并州州」
  的州，其州名为非空值，迁移逻辑不会覆盖 —— 覆盖会连玩家手动改过的名字一起冲掉。
  这些需要通过城市名横幅手动修改。

# 古代战争 3.0 v1.2.3

本次版本重点修复 RTS 军队在开战、读档、继位和指挥官死亡后失去任务的问题，补全天命衰亡与崩溃闭环，并降低寻路缓存、文化命名迁移和学校历史写入造成的持续维护开销。

## RTS 军队连续性修复

- 修复开战首轮命令在军队或目标尚未准备完成时被提前消费，导致军队长期待命的问题；未成功安装的首轮命令现在会保留并重试。
- 修复战争开始后军队仍停留在集结状态的问题；备战期结束后，未完全集结的军队也会进入进攻流程。
- 修复军队进入敌方城市区域后仍被 RTS 压制原版战斗 AI 的问题；进入 `VanillaCombat` 后释放人物任务，由原版战斗机制接管，战斗结束后恢复 RTS 战略控制。
- 修复旧存档中任务记录不完整、严格恢复失败后军队失去作战意图的问题，并增加有界的孤立军队发现与任务恢复。
- 修复国王死亡、继位完成或军团指挥官死亡后，军队无法重新绑定统帅和任务的问题。
- 修复禁卫军统帅及成员任务漂移到社交、闲逛等行为的问题，持续校验并恢复保护国王任务。
- 修复集结等待护卫人数永远不满足、军队在目标附近因端点误差停止、旧路径锚点阻止重新规划等停滞情况。
- 修复补员抵达、军队成员对账和地图军队人数显示之间的同步边界。

## 大步长调度与战时预算

- 大步长模式现在按每个逻辑步开始时的稳定待处理快照，为参战军队提供完整预算，不再只推进固定的少量军队。
- 首轮命令、军队控制、补员抵达、停滞看门狗、统帅恢复、继位恢复、生命周期发现、成员对账和数值战斗统一使用同一份执行预算。
- 新工作在处理期间重新入队时延后到下一逻辑步，避免动态队列无限排空或同一军队在一轮内重复推进。
- 原版调度模式继续使用原有小预算；全局战线规划和底层寻路仍保持有界、可续算，不会因大步长模式同步扫描全图。

## 寻路与通行缓存优化

- 修复 Actor 等待后台路径结果时每次轮询都争用会话锁的问题；仅在 worker 发布新结果后进入锁内处理。
- 增加 `WaitingInitialized` 与 `HasWorkerUpdate` 状态握手，减少多寻路线程下 Actor 主循环和 worker 对同一会话锁的竞争。
- 通行缓存维护从粗粒度 chunk 重建改为 dirty tile 增量预算，减少地图更新后的集中重建和临时分配。
- 优化通行快照与区域路径缓存的增量发布、失效和复用，避免无变化帧重复捕获整张地图数据。
- 寻路 worker 使用较低线程优先级，避免后台路径计算与游戏主线程争抢时间片。

## 运行时性能与异步写入修复

- 移除夏文化和原生夏式种族不需要的全体活人姓名迁移；已经存活的西方人物不再被批量改名，文化转换只影响后续命名和新分支。
- 修复文化命名迁移完成后被反复重新请求、重复清空候选索引并扫描全体 Actor 的幂等问题。
- 学校成员加入、学派转换、客卿离任和相关历史事件的 SQLite 事务改为后台批量写入，主线程仅进行结果提交和世界状态投影。
- 增加学校异步写入的代际校验、死亡撤销、失败清理和提交后补偿，避免人物状态变化后写入过期成员记录。
- 历史学校死亡、后代、成员索引和销毁工作改为有界队列，减少批量死亡时的同步数据库峰值。
- 增加异步提交耗时、托管堆、完整 GC、Actor 保留数量和维护队列诊断，便于区分真实后台积压与 profiler 分类归属。

## 天命衰亡与崩溃闭环

- 城市丢失和战争失败现在会按规则降低天命，并对同一年内的城市损失设置上限，避免单次连锁事件重复扣除。
- 天命进入混乱阶段后会记录连续未解决年数；持续八年未解决时执行崩溃，满足恢复条件连续三年则回到衰退阶段。
- 历史人物的天命保护改为每个天命周期仅能使用一次的宽限期，不再形成永久保护。
- 补全天命核心城市转移、战争和城市事件接线、状态持久化以及改朝换代后的状态清理。

## 继位、外交与历史显示修复

- 无有效首都时，继位流程优先使用继承人的本国家园城市完成登基；首都恢复后再统一刷新国王家园。
- 修复继承人国籍、家园、奴隶或禁卫军身份残留造成的即位失败，并加强继位后的王国身份校验。
- 修复婚盟外交仍依赖原版 clan 关系、关系资料不完整时产生拒绝或空引用的问题，改用 AW3 宗族资料和安全边界。
- 修复编年史中无王时期被错误嵌套到氏族统治时期，以及王朝归属和无王记录显示不一致的问题。
- 修复第三方 `FunBoost` 家庭补丁的特定空引用会让大步长调度器暂停整个模拟的问题；只隔离已确认的第三方异常，其他调度异常仍保持原有保护。

## 性能调查说明

- 本版本已经修复多处全图扫描、重复迁移、寻路轮询锁竞争和同步数据库写入。
- “极少单位、无战争的地图运行数年后持续掉帧”仍存在尚未归属到具体调用阶段的高频临时分配证据；本版本不宣称该现象已经彻底解决。
- 当前证据已排除 RTS、实际路径请求、异步任务积压和死亡 Actor 保留是该空世界复现的直接根因，后续将继续定位未覆盖的每帧表现链耗时。

## 安装说明

- 下载 `AncientWarfare3-v1.2.3.zip`。
- 解压后应得到单一目录 `AncientWarfare3.0`，将该目录放入 NeoModLoader 的模组目录。
- 更新前请移除旧的 `AncientWarfare3.0` 模组目录，避免旧源码残留或出现相同 GUID 的重复模组目录。
- 本发布包为源码加载包，不包含预编译的 `AncientWarfare3.dll`。
- 包内保留 `Assemblies` 及其依赖 DLL，NeoModLoader 编译源码所需依赖不可删除。

## 验证范围

- 完整规则测试。
- RTS 连续性对抗模拟。
- Release 主项目构建。
- Cultiway perf 大步长调度非回归守卫。
- 源码部署结构和必要依赖校验。
- ZIP 单一根目录、源码白名单、版本号、依赖程序集及逐文件 SHA-256 校验。

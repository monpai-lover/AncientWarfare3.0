# 古代战争 3.0 v1.2.7b

v1.2.7 之后的维护版本，打包 `b` 分支当前内容。以修复继承、官职品阶、
夏人贴图和运行时性能为主，没有新增系统。

## 继承与太子

- **继承池现在会收直系后裔**。原来只收子、兄弟侄、叔伯堂兄弟三类，孙辈
  只能靠档案表碰运气，所以嫡长子早卒时嫡长孙进不了顺位。现在逐辈往下走
  （最多六辈），**穿过已故的中间辈**，承重孙不会再被漏掉。
- 顺位排序补上嫡庶：同一支同一档内先比嫡庶，再比长幼。原来靠加权分数里
  的大权重「压」过去，边界上会翻盘。
- 同胞兄弟不再要求与君主同国。在本国的优先，本国没有活人时旅居国外的兄长
  仍可继位。
- **只读算出来的继承人现在会被真正登记**。王国窗口的太子头像走的是只读
  计算，那条路从不落盘 —— 所以归化、太子身份、族谱称号全部为空。这是
  「看得到太子但国籍不通、身份写不进去」的根因。
- 册立只以国籍为硬指标；居所尽力而为，学派籍贯不通只告警不阻断。原来要求
  必须迁进都城，失败时什么都不写。
- 每年校验一次继承人国籍，漂到别国的会重新归化。
- 太子不再兼任郡县长官，册立时自动卸任。
- 新增继承人所属王国记录，族谱 tooltip 和角色面板的「太子」称号按继承的
  王国解析，不再跟着人当前所在国走。

## 官职品阶

- **自定义官职的「最低任职品阶」终于生效**。这个字段一直只被写入和校验，
  没有任何代码读过 —— 填 18 和不填完全一样。
- 任命在本模组里等于提拔（允许越级），所以它是**任职品阶下限**而不是准入
  门槛：品阶不够的人照样能被任命，任命成立后把仕途等级抬到这个数。
- 原先决定任命品阶的是一张写死的表，只认官职等级 10 / 20 / 30 三档
  （中央 14 / 12 / 11，地方 5 / 1 / 1）。自定义官场填其他数字时一律落到
  一品 —— 这就是「所有品级都能进入」的来源。
- 两个字段改名以对应实际行为：**品级 → 官职等级**，
  **最低仕途等级 → 最低任职品阶**。说明文本一并重写（旧文案写着「是任命
  门槛而不是官职品级」，语义正好相反）。

## 官署与郡县

- 空缺补任改为事件驱动，县令的任命与回读、地方官署显示、历任记录一并修复。
- 郡县长官不再出现在中央官场。
- 流转官员的仕途变动会被正确记录。

## 夏人贴图

- 修复夏人君主与继承人头部加载，同步继承人贴图偏移。
- 四倍君主头像改用 pixelsPerUnit 缩放，渲染倍率与普通角色相互独立。
- 按品阶分级的夏人官服恢复：高阶紫、中阶红、低阶绿。
- 修复夏人望楼的王国归属，并在正确的读档阶段清理无主建筑。

## 性能

- 继承相关：君主父系祖先表按「王国 + 参照君主」共享一份（原来每次刷新建
  一次、征兵时**每名壮丁**再建一次）；父亲 id 记忆化；亲缘上下文缓存；
  重排时不再重算整套候选人事实。
- 官场、共和选民团、将领名单、城市候选序列改为持久化 + 事件维护。
- 法理州维护、学派场所缓存、SQLite 页缓存、延迟队列排空等多处收敛。
- 嵌套附庸纳入王国图集范围，法理地图在多层附庸下保持连续。

## 鸣谢

- 刘季 赞助累计 100。
- vader 提供了大量人物与建筑贴图。

## 打包

- 包名：`AncientWarfare3-v1.2.7b.zip`
- 分发的是**源码 + 运行时资源**，不含预编译的 `AncientWarfare3.dll`；
  NeoModLoader 在游戏启动时编译。
- 安装：把单个 `AncientWarfare3.0` 目录放进 NeoModLoader 的 `Mods` 目录。

## 验证

- `dotnet build AncientWarfare3.csproj` — 0 警告 0 错误。
- `Tests/AncientWarfare3.Rules.Tests` 的规则与源码守卫测试。
- 说明：本版的性能改动是按调用次数做的静态分析，**没有 before/after 计时
  数据**；自定义官职品阶下限也**尚未实机验证**。

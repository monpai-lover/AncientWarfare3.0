# 古代战争 3.0 v1.2.1

本次版本是在 `v1.2.0` 基础上的功能与稳定性更新，重点加入军事节度使体系，并从继承人登记阶段、国王死亡处理和大步长调度边界三个层面修复继位卡顿与模拟暂停问题。

## 主要新功能

### 军事节度使

- 新增军事节度使附庸关系、创建入口、管理窗口、状态持久化和存档恢复。
- 节度使拥有独立辖区、继承和独立逻辑，并按规则承担宗主国的战争义务。
- 节度使疆域复用宗主国颜色，同时保留自身边界、国号和关系标识。
- 节度使关系结束、独立或恢复存档时会清理旧的颜色、名称和多人投影状态，避免残留。
- 节度使国王与指定留后会在宗主国官场中显示为军事节点。
- 节度使国王和留后保留真实身份与继承逻辑，但地图、人物头像和官场节点统一使用军人外观。
- 新增可复用的视觉角色切换器，支持默认、平民、军人、领主和国王外观，不改变人物实际身份。

### 权威继承人流程

- 已登记且仍有效的继承人成为唯一优先继位人，国王死亡时不再重复计算候选人。
- 继承人从被任命时起就会修正王国、城市、军队、禁卫军和官职身份，提前消除继位失败隐患。
- 正式继位统一复用原版成王工具，模组只负责准备身份和验证结果。
- 只有继承人缺失或失效时才调用既有继承算法：先宗族直系，再旁系过继，宗族灭绝后改朝换代。
- 使用“有现任国王的王族”索引过滤普通家族，同时允许同一家族存在多个在位国王。
- 无有效首都时允许继承人在自己的家园城市完成继位；首都恢复后统一刷新国王家园。
- 多人模式的继承人安装、权威命令和战略状态投影使用同一套继位流程。

## 性能与稳定性修复

### 国王死亡与继位

- 为同一死王增加幂等状态，避免死亡补丁和原版王国检查重复处理。
- 继承人没有变化时跳过全国家标记清理、谱系维护、学校、医疗、档案和 UI 索引重建。
- 预计算继承所需快照与关系索引，减少死亡帧中的全量 Actor、城市、官员和谱系扫描。
- 将继承争议的 SQLite 写入移出死亡主路径，并使用异步持久化提交。
- 消除候选人逐个追溯谱系产生的 N+1 查询，复用存活继承关系和王族索引。
- 移除国王死亡路径中无条件执行的科举 SQL；仅在确有待处理殿试状态时进入对应逻辑。
- 死亡档案改为追踪实际提交结果并分批排空，避免积压、重复写入和死亡高峰卡顿。

### 大步长调度器一致性

- 将王权、继承、附庸等权威维护拆成可协作让出的有预算工作，避免单帧集中完成。
- 修复 Actor 批量死亡后过期落地回调继续访问空对象的问题。
- 修复人物从地块移除后区域投影仍保留旧引用的问题。
- 修复 Actor 计时容器在后台批次间交叉写入导致的一致性异常。
- 修复节度使计数器、多人副本和存档恢复状态跨世界或跨会话残留的问题。
- 保留已移植的 Cultiway perf 调度边界，权威状态只在允许的提交阶段写入，不把 Unity 对象写入迁移到后台线程。

## 界面与兼容修复

- 外交王国列表新增滚动支持，国家数量较多时可以完整选择目标。
- 补全用户反馈中缺失的事件、任务和继位错误本地化文本。
- 修复西方文化人物无法进行文官任命的问题。
- 修复节度使关系、战争目标和附庸状态在界面中的显示与点击行为。
- 更新支持者名单，并将“一米”列为寻路与大步长调度器的技术支持者。

## 安装说明

- 下载 `AncientWarfare3-v1.2.1.zip`。
- 解压后应得到单一目录 `AncientWarfare3.0`，将其放入 NeoModLoader 的模组目录。
- 本发行包继续采用源码加载结构，不包含编译后的 `AncientWarfare3.dll`。
- 更新前建议备份存档并移除旧模组目录，再放入新目录，避免旧源码残留。

## 验证范围

- 完整规则测试与源码守卫。
- Release 主项目构建。
- 军事节度使创建、继承、战争、持久化、界面和外观专项守卫。
- 权威继承、死王性能、死亡档案提交和多人投影专项守卫。
- Cultiway perf 大步长调度非回归守卫。
- 发布包、部署目录与 `master` 发布清单的逐文件 SHA-256 校验。

# v1.1.2 Release Root-Cause Copy Design

## Goal

Expand the existing `v1.1.2` GitHub Release description with concrete root-cause information for the reserve recruitment, post-war army return, war presentation, and Mandate recovery fixes.

## Scope

- Preserve the current release title, assets, tag, installation instructions, and existing body text.
- Append the new entries to the existing `## 根因修复` section.
- Append one verification entry to the existing `## 验证` section.
- Do not rewrite unrelated release notes or change repository source code.

## Root-Cause Additions

- 预备兵池：修复容量统计遗漏已动员人员、读档后兵池状态不完整、征召失败人员未正确回池，避免战备期长期不征兵或重复超额补充。
- 战后返乡：修复战争结束仅清除任务却没有建立返乡状态的问题；军团可跨海调用运输系统回国，新战争接管时会取消旧返乡任务。
- 战争展示：修复结算与编年史使用占位战争名、白和平与朝贡文本不准确的问题，统一读取真实战争名称和结果。
- 天命继承：修复数据库继承成功后，运行态、帝号、年号、历史记录可能因后续步骤失败而永久缺失的问题，引入可重试持久化流程。
- 法律核心：修复旧天命核心尚未写入时被新天命替换，导致两代核心同时丢失的问题；新天命会继承已落库和待处理快照，历史补写不会覆盖当前天命状态。
- 王朝国号：修复王朝建立成功但国号投影失败后无法重试，以及旧存档从实时领土错误重建历史核心的问题。

## Verification Addition

- 预备兵池、战争返乡与天命继承三个专项测试及完整规则测试通过；部署的 3239 个源码与资源文件均通过相对路径和 SHA-256 一致性校验。

## Update And Verification

1. Read the current release body immediately before editing.
2. Insert the approved text without removing existing content.
3. Update only release `v1.1.2` through the GitHub CLI.
4. Read the release back from GitHub and verify the six root-cause entries and the deployment verification entry are present exactly once.
